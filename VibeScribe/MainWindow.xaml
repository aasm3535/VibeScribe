<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="VibeScribe.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VibeScribe"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="VibeScribe">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock
        Grid.Column="0"
        Text="All Recording"
        VerticalAlignment="Top"
        HorizontalAlignment="Left"
        Margin="10,10,0,0"/>

        <Button
        Grid.Column="1"
        Content="Record"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Click="RecordButton_Click" />

        <Button Grid.Column="2" Click="NewRecordingButton_Click" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,10,10,0">
            <StackPanel Orientation="Horizontal">
                <SymbolIcon Symbol="Add" />
                <TextBlock Text="New Recording" />
            </StackPanel>
        </Button>

    </Grid>
</Window>