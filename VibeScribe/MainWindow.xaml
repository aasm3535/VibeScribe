<Window
    x:Class="VibeScribe.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VibeScribe"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="VibeScribe">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Button x:Name="NewRecordButton" Command="{x:Bind ViewModel.NewRecordCommand}" Grid.Row="0" HorizontalAlignment="Right" Margin="10">
            <ToolTipService.ToolTip>
                <ToolTip Content="Start a new recording"/>
            </ToolTipService.ToolTip>
            <StackPanel Orientation="Horizontal">
                <FontIcon Glyph="{x:Bind ViewModel.RecordIconGlyph, Mode=OneWay}"/>
                <TextBlock Text="{x:Bind ViewModel.RecordButtonText, Mode=OneWay}"/>
            </StackPanel>
        </Button>

        <Frame x:Name="MainFrame" Grid.Row="1"/>

        <InfoBar x:Name="TranscriptionInfoBar" Grid.Row="2" IsOpen="False" Severity="Informational" Title="Transcription Ready" Message="Your new transcription is ready."/>
    </Grid>
</Window>