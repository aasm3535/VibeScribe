<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="VibeScribe.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VibeScribe"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="VibeScribe">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Верхняя панель: All Records слева, New Recording справа -->
        <StackPanel Grid.Row="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Stretch">
            <TextBlock Text="All Records"
                       VerticalAlignment="Center"
                       Margin="10,10,0,10"
                       FontSize="18"
                       FontWeight="Bold" />
            <Border Grid.Column="2" HorizontalAlignment="Stretch" Margin="0,10,10,10">
                <Button x:Name="NewRecordingButton" Click="NewRecordingButton_Click" Background="Transparent" BorderThickness="0" HorizontalAlignment="Right">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon x:Name="RecordingIcon" FontFamily="Segoe MDL2 Assets" Glyph="&#xEA3F;" FontSize="12" Margin="0,0,4,0" />
                        <TextBlock x:Name="RecordingTextBlock" Text="New Recording" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </Border>
        </StackPanel>

        <!-- Левая панель: История записей -->
        <Border Grid.Row="1" Grid.Column="0" Background="LightGray" BorderBrush="Gray" BorderThickness="1,0,1,0" Width="300">
            <StackPanel>
                <TextBlock Text="История записей" Margin="10" FontWeight="Bold" />
                <ListView x:Name="RecordsListView" Margin="10" ItemClick="RecordsListView_ItemClick" SelectionMode="Single">
                    <!-- Items будут добавлены из кода -->
                </ListView>
            </StackPanel>
        </Border>

        <!-- Бордер между панелями -->
        <Border Grid.Row="1" Grid.Column="1" BorderBrush="Gray" BorderThickness="1,0,0,0" Width="1" Background="Transparent" />

        <!-- Правая панель: Просмотр транскрипта -->
        <Border Grid.Row="1" Grid.Column="2" Background="White" Margin="10,0,10,10">
            <StackPanel Margin="10">
                <TextBox x:Name="TitleTextBox" Text="Название записи" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" IsReadOnly="True" Background="Transparent" BorderThickness="0" />
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="TranscriptTextBlock" Text="Выберите запись для просмотра транскрипта" TextWrapping="Wrap" FontSize="14" />
                </ScrollViewer>
                <TextBlock x:Name="StatusTextBlock" Text="Ready to record" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,10,0,0" FontSize="12" Foreground="Gray" />
            </StackPanel>
        </Border>

    </Grid>
</Window>