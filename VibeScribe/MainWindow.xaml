<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="VibeScribe.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VibeScribe"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="VibeScribe">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <StackPanel Margin="10">
        <!-- Header -->
        <TextBlock Text="All Recording"
                   VerticalAlignment="Center"
                   Margin="0,0,0,10"
                   FontSize="18" />

        <!-- Records History -->
        <TextBlock Text="Records History" Margin="0,0,0,10" FontWeight="Bold" />
        <ListView x:Name="RecordsListView" Margin="0,0,0,20" ItemClick="RecordsListView_ItemClick" SelectionMode="Single">
            <!-- Items will be added from code -->
        </ListView>

        <!-- Transcript Viewer -->
        <TextBox x:Name="TitleTextBox" Text="Recording Title" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" IsReadOnly="True" Background="Transparent" BorderThickness="0" />
        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,0,0,10">
            <TextBlock x:Name="TranscriptTextBlock" Text="Select a recording to view the transcript" TextWrapping="Wrap" FontSize="14" />
        </ScrollViewer>
        <Button x:Name="NewRecordingButton" Click="NewRecordingButton_Click" Background="Transparent" BorderThickness="0" HorizontalAlignment="Center" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal">
                <FontIcon x:Name="RecordingIcon" FontFamily="Segoe MDL2 Assets" Glyph="&#xEA3F;" FontSize="12" Margin="0,0,4,0" />
                <TextBlock x:Name="RecordingTextBlock" Text="New Recording" VerticalAlignment="Center" />
            </StackPanel>
        </Button>
        <TextBlock x:Name="StatusTextBlock" Text="Ready to record" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,10,0,0" FontSize="12" Foreground="Gray" />
    </StackPanel>
</Window>